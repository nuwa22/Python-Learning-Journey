# we use __init__.py file to make a directory a package. It can be an empty file or
# it can contain code that we want to execute when the package is imported. We can also
# use it to define the __all__ variable which is a list of modules that we want to be
# imported when we use from package import * statement.